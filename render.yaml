services:
  - type: web
    name: visual-director-relay
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build
    startCommand: npm run relay:start
    healthCheckPath: /health
    envVars:
      - key: VISUAL_RELAY_HOST
        value: 0.0.0.0
